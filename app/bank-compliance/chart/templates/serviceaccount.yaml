# ==============================================================================
# Azure Workload Identity Federation (WIF) ServiceAccount
# Purpose:
#   Eliminates stored secrets/credentials in Kubernetes. The client-id annotation
#   binds this Kubernetes ServiceAccount to Azure Entra ID Managed Identity (UAMI)
#   via OIDC token exchange for passwordless Key Vault & Azure OpenAI access.
# ==============================================================================
{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.serviceAccount.name }}
  namespace: {{ .Release.Namespace }}
  annotations:
    # Binds pod to Azure User-Assigned Managed Identity Client ID
    azure.workload.identity/client-id: {{ .Values.serviceAccount.clientId | quote }}
  labels:
    azure.workload.identity/use: "true"
    app.kubernetes.io/name: {{ include "bank-compliance.name" . }}-sa
    {{- include "bank-compliance.labels" . | nindent 4 }}
{{- end }}
