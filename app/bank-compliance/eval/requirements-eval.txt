# AI Evaluation Dependencies
# Used ONLY in CI evaluation jobs — NOT installed in the backend Docker image.
azure-ai-evaluation>=1.0.0,<2.0.0
azure-identity>=1.19.0,<2.0.0
openai>=1.56.0,<2.0.0
httpx>=0.27.0
