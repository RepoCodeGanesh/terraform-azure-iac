apiVersion: v1
kind: ConfigMap
metadata:
  name: bankc-backend-config
  namespace: bank-compliance
data:
  CONTENT_SAFETY_ENDPOINT: "https://cs-ht-bankc-p-sea-01.cognitiveservices.azure.com/"

  # Internal cluster DNS
  LITELLM_URL: "http://litellm:4000/v1"
  OPENAI_MODEL: "gpt-5.4-nano"
  QDRANT_HOST: "qdrant"
  QDRANT_PORT: "6333"
  QDRANT_COLLECTION: "rbi_master_directions"

  # CORS: includes both default Azure SWA domain, optional custom domain, and local dev
  ALLOWED_ORIGINS: "https://happy-coast-00666250f.7.azurestaticapps.net,https://bank.mytaxbot.site,http://localhost:3000"

  APPLICATIONINSIGHTS_CONNECTION_STRING: ""
  ENVIRONMENT: "production"
