# ==============================================================================
# Workflow: PR Title Conventional Commits Check (Caller Workflow)
# Calls central enterprise standard: RepoCodeGanesh/.github
# ==============================================================================

name: PR Title Check

on:
  pull_request:
    types: [opened, edited, synchronize, reopened]

permissions:
  pull-requests: read
  statuses: write

jobs:
  validate-pr-title:
    name: Validate PR Title
    uses: RepoCodeGanesh/.github/.github/workflows/pr-title-check.yml@main
    with:
      require_scope: false
